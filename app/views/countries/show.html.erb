<h1><%= @country.name %></h1>

<p>
  Best restaurants in <%= @location %>
  <% unless current_user %>
    (<%= link_to "sign in with Facebook", "/auth/facebook" %> to change your location.)
  <% end %>
</p>


<ul>
  <% @results.each do |result| %>
    <li>
      <%= image_tag(result[:rating_img_url]) %>
      <%= link_to result[:name], result[:url] %>
    </li>
  <% end %>
</ul>
